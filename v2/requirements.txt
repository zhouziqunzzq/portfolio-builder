# Core data stack
pandas>=2.2
numpy>=1.26

# Market data provider
yfinance>=0.2.40

# HTTP and HTML parsing for S&P 500 scraper
requests>=2.32.0
lxml>=4.9.3

# Plotting (for sector weights visualization)
matplotlib>=3.8

# Parquet backend for caching (pandas.to_parquet)
pyarrow>=14.0.0

# YAML config
pyyaml>=6.0

# Alpaca SDK (IML/EML playgrounds)
alpaca-py>=0.42

# Env file loader for manual scripts
python-dotenv>=1.0

# Observability (OpenTelemetry metrics -> OTLP)
opentelemetry-api>=1.25
opentelemetry-sdk>=1.25
opentelemetry-exporter-otlp-proto-grpc>=1.25
